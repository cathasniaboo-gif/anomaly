# Copy to .env for local dev. Expo automatically inlines any EXPO_PUBLIC_*
# variable into the JS bundle at build/start time — no extra config needed.
#
# Leave unset to run fully offline against the bundled seed data in
# src/data/updates.ts (this is the default experience, not a degraded one).
EXPO_PUBLIC_API_BASE_URL=
